name: Run Zoho Script Hourly

on:
  schedule:
    - cron: "0 * * * *"   # تشغيل كل ساعة
  workflow_dispatch:       # لتشغيله يدويًا لو أردت

jobs:
  run-script:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests pandas

      - name: Run Zoho Fetch Script
        env:
          CLIENT_ID: ${{ secrets.CLIENT_ID }}
          CLIENT_SECRET: ${{ secrets.CLIENT_SECRET }}
          REFRESH_TOKEN: ${{ secrets.REFRESH_TOKEN }}
          ORG_ID: ${{ secrets.ORG_ID }}
        run: |
          python code.py

      - name: Commit & Push Output JSON
        if: success()    # ينفذ فقط لو السكربت اشتغل بدون أخطاء
        run: |
          git config --global user.name "github-actions"
          git config --global user.email "actions@github.com"
          git add -A
          git commit -m "Auto-update invoices JSON [CI SKIP]" || echo "No changes"
          git push
